# Supabase Keep-Alive

Prevent Supabase free-tier projects from pausing. GitHub Actions cron script.

## Masalah
Supabase free tier auto-pause project setelah **7 hari inactivity**.

## Cara Kerja
GitHub Actions cron tiap hari → query REST API Supabase → project tetap aktif.

## Setup

1. **Push repo ini ke GitHub**

2. **Bikin tabel di setiap Supabase project** (sekali aja):
```sql
CREATE TABLE IF NOT EXISTS "keep-alive" (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY,
  name TEXT NULL DEFAULT ''::text,
  CONSTRAINT "keep-alive_pkey" PRIMARY KEY (id)
);

INSERT INTO "keep-alive" (name) VALUES ('placeholder');
```

3. **Set GitHub Secrets**  
   Settings → Secrets and variables → Actions → New repository secret:

   | Secret | Value |
   |---|---|
   | `SUPABASE_URL_1` | `https://xxxx.supabase.co` |
   | `SUPABASE_KEY_1` | `eyJhbG...` (anon/public key) |
   | `SUPABASE_URL_2` | URL project kedua (opsional) |
   | `SUPABASE_KEY_2` | Key project kedua (opsional) |
   | ... | Tambahin terus (`_3`, `_4`, dst) |

4. **Done.** Cron jalan tiap hari jam 06:00 UTC.

   Tes manual: **Actions → Supabase Keep-Alive → Run workflow → Run workflow**

## Testing Lokal

```bash
SUPABASE_URL_1="https://xxx.supabase.co" \
SUPABASE_KEY_1="eyJhbG..." \
python3 scripts/keep-alive.py
```

## Customisasi

- **Ubah jadwal:** edit `cron` di `.github/workflows/keep-alive.yml` — format cron 5 field
- **Ubah tabel/kolom:** tambahin secret `KEEP_ALIVE_TABLE` dan `KEEP_ALIVE_COLUMN`
- **Nambah project:** edit workflow YAML, tambahin `SUPABASE_URL_6`, `SUPABASE_KEY_6`, dst

## Struktur

```
supabase-keep-alive/
├── .github/workflows/keep-alive.yml
├── scripts/keep-alive.py
└── README.md
```
